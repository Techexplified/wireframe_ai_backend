// ═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
// ─── modules/credits/credit.types.ts — Credit Reservation & Refund Types
// ═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
//
// PURPOSE:\n//   TypeScript interfaces for credit system\n//   Covers credit reservation (prevent double-charging) and refund mechanisms\n//\n// CREDIT SYSTEM OVERVIEW:\n//   \n//   Problem: AI generation consumes variable tokens\n//     • Budget middleware estimates tokens (before API call)\n//     • But actual tokens only known after generation completes\n//     • If we deduct estimated credits upfront, might overcharge or undercharge\n//   \n//   Solution: Credit Reservation FSM\n//     1. Reserve (deduct estimated amount) — prevents double-charging\n//     2. Process (call OpenRouter, stream generation) — happens in background\n//     3. Settle (update reservation with actual amount) — after stream completes\n//     4. Refund (undo reservation if generation fails) — on error or client disconnect\n//   \n//   Why important?\n//     • Prevents: user seeing \"generation complete\" then \"insufficient credits\" error\n//     • Prevents: UI inconsistency (showing credits then revealing shortage)\n//     • Prevents: double-charging if generation fails mid-stream\n//\n// CREDIT RESERVATION (CreditReservationResult):\n//   \n//   Returned by credit.controller.ts → reserveCredits() function\n//   Called at START of /generate/start before calling OpenRouter\n//   \n//   Fields:\n//   \n//   success (boolean):\n//     • true = reservation succeeded, credits deducted from balance\n//     • false = reservation failed (insufficient credits, error, etc.)\n//     • If false: entire generation should abort, return 402 or 403 to client\n//   \n//   creditsLeft (integer):\n//     • User's remaining plan_credits after reservation\n//     • Example: user had 100 plan credits, reserved 5 → creditsLeft=95\n//     • Sent to client via HTTP header X-Credits-Left (for UI counter update)\n//     • If creditsLeft reaches 0 after reserve: UI should disable generate button\n//   \n//   topup_creditsLeft (integer):\n//     • User's remaining topup_credits after reservation\n//     • These are purchased credits (never reset, permanent until used)\n//     • Shown separately from plan_credits in UI\n//     • Used for: user awareness (\"You have 50 top-up credits\")\n//   \n//   pool ('plan' | 'topup'):\n//     • Which credit pool was charged for this reservation\n//     • 'topup' = topup_credits were deducted (purchased credits)\n//     • 'plan' = plan_credits were deducted (monthly credits)\n//     • Shown in analytics (\"User used more topup than plan\")\n//     • Used for: settlement (settle from same pool)\n//   \n//   reservationId (string, UUID):\n//     • Unique identifier for this reservation\n//     • Fix CREDIT-C-01: server-generated UUID (not client-supplied)\n//     • Used to: authorize refunds (/generate/refund endpoint)\n//     • Stored in credit_reservations collection\n//     • Prevents: clients refunding arbitrary amounts (\"refund me 1000 credits\")\n//     • Example: reservationId = \"abc-123-def-456\"\n//   \n//   Reservation Lifecycle:\n//     1. Create: insert { reservationId, userId, amount, pool, status: 'reserved' } to DB\n//     2. Deduct: update user.plan_credits or user.topup_credits (atomically with insert)\n//     3. Client receives: reservationId in response header X-Reservation-Id\n//     4. If /generate/start succeeds:\n//        → ai.controller calls completeEventProcessed() in webhook\n//        → Updates reservation.status = 'completed', reservation.actual_cost = tokens\n//     5. If /generate/start fails mid-stream:\n//        → Client calls /generate/refund with reservationId\n//        → credit.controller looks up reservation by ID\n//        → Verifies not already refunded (status='refunded')\n//        → Adds back credits to user (using original amount)\n//        → Updates status = 'refunded'\n//     6. If refund expires (no action for 24 hours):\n//        → Automatic cleanup job converts expired 'reserved' to 'abandoned'\n//        → Credits NOT returned (client forgot to refund)\n//        → Analytics: track abandoned reservations\n//
// REFUND REQUEST (RefundRequest):\n//   \n//   Sent by client to /api/features/generate/refund endpoint\n//   Used when: generation failed mid-stream and user wants credits back\n//   \n//   Fields:\n//   \n//   reservationId (string):\n//     • The UUID returned in X-Reservation-Id header from /generate/start\n//     • Example: \"abc-123-def-456\" (same as reservation.reservationId)\n//     • ONLY field accepted (no cost, pool, etc.)\n//     • Fix CREDIT-C-01: Server looks up amount from DB, not from client\n//   \n//   Fix CREDIT-C-01 Explanation:\n//     \n//     Before:\n//       • Client sent: { reservationId, amount, pool }\n//       • Problem: Client could lie (\"refund me 1000 credits\")\n//       • Attacker flow: user=123 generates (reserves 5), calls refund with amount=1000\n//       • Result: user gets +995 undeserved credits\n//   \n//     After:\n//       • Client sent: { reservationId }\n//       • Server looks up from DB: INSERT found (amount=5, pool='topup')\n//       • Server refunds: add 5 back to user.topup_credits\n//       • Attacker can't lie: server knows the truth\n//   \n//   Refund Validation:\n//     1. Look up reservationId in credit_reservations collection\n//     2. Verify: exists and belongs to current user\n//     3. Verify: status is 'reserved' (not already refunded, completed, or failed)\n//     4. Verify: created within last 24 hours (prevent late refunds)\n//     5. Extract: amount, pool, user_id, original_credits_balance\n//     6. Perform refund: user.topup_credits += amount (or plan_credits)\n//     7. Mark: reservation.status = 'refunded', reservation.refunded_at = now\n//     8. Return: { success: true, credits_restored, credits_left }\n//   \n//   Refund Error Cases:\n//     • Reservation not found → 404 Not Found\n//     • Already refunded → 409 Conflict (\"Already refunded\")\n//     • Already completed → 409 Conflict (\"Generation succeeded, can't refund\")\n//     • Expired (>24h old) → 410 Gone (\"Refund window expired\")\n//     • Database error → 500 Internal\n//
// DATABASE SCHEMA (credit_reservations collection):\n//   \n//   Document:\n//     {\n//       _id: ObjectId,\n//       reservationId: \"abc-123-def-456\",  // UUID, unique index\n//       firebaseUid: \"user_123\",\n//       figmaUserId: \"figma_user_456\",\n//       amount: 5,                          // Credits reserved\n//       pool: \"topup\" | \"plan\",           // Which pool\n//       status: \"reserved\" | \"completed\" | \"refunded\" | \"failed\" | \"abandoned\",\n//       original_balance: 100,              // User's balance before reserve\n//       actual_cost: 4.8,                   // Filled by settlement (after generation)\n//       refunded_at: ISODate(...),          // When refund happened\n//       generationId: \"gen_123\",           // Linked to which generation\n//       createdAt: ISODate(\"2024-01-15T12:34:56Z\"),\n//       updatedAt: ISODate(\"2024-01-15T12:34:57Z\")\n//     }\n//   \n//   Indexes:\n//     • Unique index on reservationId (prevent duplicates)\n//     • Index on firebaseUid (lookup user's reservations)\n//     • Index on status (query reserved vs. completed)\n//     • Index on createdAt (find expired reservations)\n//     • TTL index on createdAt → auto-delete after 30 days\n//
// FLOW EXAMPLE (complete credit cycle):\n//   \n//   1. User clicks Generate button\n//      • Client calls POST /api/features/generate/start\n//      • Budget middleware: score=6, tokenBudget=1800, cost_estimate=0.08\n//   \n//   2. ai.controller reserves credits:\n//      • Calls credit.controller.reserveCredits(amount=5, pool='topup')\n//      • DB inserts: { reservationId: 'xyz-789', firebaseUid, amount: 5, status: 'reserved' }\n//      • DB updates: user.topup_credits -= 5 (atomic transaction)\n//      • Returns: { success: true, creditsLeft: 45, reservationId: 'xyz-789' }\n//   \n//   3. ai.controller returns to client:\n//      • HTTP 200 OK\n//      • X-Reservation-Id: xyz-789 (client saves this)\n//      • X-Credits-Left: 45 (UI updates counter)\n//      • Content-Type: text/event-stream\n//      • Begins streaming HTML chunks\n//   \n//   4a. SUCCESS PATH: Generation completes\n//      • ai.telemetry.ts calculates: actual_tokens=1234\n//      • Settlement: actual_cost = 1.23 credits\n//      • ai.controller calls: completeEventProcessed(reservationId)\n//      • DB updates: reservation.status='completed', reservation.actual_cost=1.23\n//      • No further action: credits remain deducted (1.23)\n//      • Client receives: final SSE event with complete HTML\n//   \n//   4b. ERROR PATH: Generation fails mid-stream\n//      • OpenRouter returns error or stream cuts off\n//      • ai.controller throws error\n//      • error.middleware returns 500 to client\n//      • Client receives: partial HTML + error\n//      • Client calls: POST /api/features/generate/refund { reservationId: 'xyz-789' }\n//      • credit.controller processes refund:\n//        → Looks up reservation by ID\n//        → Verifies status='reserved', not yet completed/refunded\n//        → Adds 5 back to user.topup_credits\n//        → Updates reservation.status='refunded'\n//      • Client receives: { success: true, credits_restored: 5, creditsLeft: 50 }\n//      • User back to 50 topup credits (as if reservation never happened)\n//   \n//   4c. INCOMPLETE PATH: Client closes browser mid-stream\n//      • Stream cut off, reservation still status='reserved'\n//      • User has 45 topup credits (5 reserved, frozen)\n//      • After 24 hours: cleanup job marks as 'abandoned'\n//      • User loses those 5 credits (no refund window anymore)\n//      • Analytics: count abandoned = lost revenue opportunity\n//\n// SETTLEMENT vs. REFUND:\n//   \n//   Settlement (completeEventProcessed):\n//     • Called after successful generation\n//     • Updates reservation.status = 'completed'\n//     • Updates reservation.actual_cost = real tokens used\n//     • Credits stay deducted (user consumed them)\n//   \n//   Refund (via /generate/refund endpoint):\n//     • Called after failed generation\n//     • Updates reservation.status = 'refunded'\n//     • Adds credits back to user's balance\n//     • Happens within 24-hour window\n//
// ATOMIC OPERATIONS:\n//   \n//   Reserve must be atomic (prevent race conditions):\n//     • Check: user.topup_credits >= amount\n//     • Deduct: user.topup_credits -= amount\n//     • Insert: reservation document\n//     • All in one transaction (MongoDB session)\n//     • If any step fails: entire operation rolled back\n//   \n//   Refund must be atomic:\n//     • Read: reservation by ID\n//     • Verify: not already refunded, not too old\n//     • Add back: user credits += amount\n//     • Update: reservation.status = 'refunded'\n//     • All in one transaction\n//   \n//   Why? Prevent:\n//     • User reserved 5, server crashes before deducting → user not charged\n//     • User refunds, server crashes before adding back → credits lost\n//     • Two refunds processed for same reservation → double-refund\n//

export interface CreditReservationResult {
  success:           boolean;
  creditsLeft:       number;
  topup_creditsLeft: number;
  pool:              'plan' | 'topup';
  reservationId:     string;   // Fix CREDIT-C-01: UUID used to authorize refund
}

// Fix CREDIT-C-01: Refund no longer accepts client-supplied cost/pool.
// Only reservationId is accepted — server looks up the authoritative amount.
export interface RefundRequest {
  reservationId: string;
}
